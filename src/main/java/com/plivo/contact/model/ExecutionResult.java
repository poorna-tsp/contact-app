package com.plivo.contact.model;

public enum ExecutionResult {
    SUCCESS,
    DUPLICATE_ACCOUNT,
    ACCOUNT_NOT_PRESENT,
    PASSWORD_INCORRECT,
    INVALID_TOKEN,
    INVALID_USER,
    INVALID_PWD
}
